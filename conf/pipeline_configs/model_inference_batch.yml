mlflow_params:
  model_registry_stage: 'production'

data_input:
  # Require DataFrame to score the model on - must contain column(s) for lookup keys
  # to join feature data from Feature Store
  table_name: 'daiwt_mlops_prod_paris.churn_labels_paris'

data_output:
  mode: 'overwrite'
