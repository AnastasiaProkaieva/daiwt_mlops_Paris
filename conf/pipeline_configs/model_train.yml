mlflow_params:
  run_name: 'deepest_forest'

pipeline_params:
  test_size: 0.25
  random_state: 42

model_params:
  n_estimators: 100
  max_depth: 21
  min_samples_leaf: 1
  max_features: 'auto's
  random_state: 42
